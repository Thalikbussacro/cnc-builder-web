# ==============================================
# Frontend Environment Variables (CLEANED)
# Sensitive keys moved to backend for security
# ==============================================

# API Backend URL
NEXT_PUBLIC_API_URL=http://localhost:3001

# Supabase (Client-side access only - anon key is safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Google OAuth (Client ID for client-side OAuth flow)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id
